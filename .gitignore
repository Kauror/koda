node_modules/
.next/
out/
build/
dist/

.env
.env.local
.env.production

npm-debug.log*
yarn-error.log*

.DS_Store
*.tsbuildinfo
next-env.d.ts

.claude/settings.local.json

# Local merge-ready import source files and generated QA reports (kept out of git).
# See data/import/README.md for what goes here.
data/import/*.xlsx
data/import/*.txt
!data/import/koda_taxonomy_rules_v1_2.txt
data/import/reports/
data/import/bundles/
data/import/backups/
data/archive/

# Local PGlite verification database (machines without Docker/Postgres).
.pglite/
.pglite-*/

# Loose local data-pipeline artifacts / handoff packages dropped in the repo
# root (Excel workbooks, audit reports, delivery ZIPs, old package versions).
# These are data, not source, and must never be committed. The real import
# files live in data/import/ (ignored above). Files stay on disk; git ignores them.
/*.xlsx
/*.csv
/*.zip
/vanemad/
/KODA_NEXT_CHAT_HISTORY_OVERVIEW.md
/koda_taxonomy_rules_*.txt
